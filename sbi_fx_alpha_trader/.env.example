############################################
# SBI FX α Auto Trader - env example
############################################

# --- Mode ---
# paper: generate synthetic ticks, never sends orders
# live : connects to your configured endpoints and sends orders
TRADER_MODE=paper

# --- General ---
SYMBOL=USDJPY
ORDER_QTY=1000
TIMEFRAME_SECONDS=60
FAST_SMA=10
SLOW_SMA=30
MAX_NET_POSITION=1000

# --- OAuth / Token ---
SBI_CLIENT_ID=your_client_id
SBI_CLIENT_SECRET=your_client_secret
SBI_TOKEN_URL=https://api.example.com/oauth/token
SBI_OAUTH_GRANT_TYPE=client_credentials
SBI_OAUTH_SCOPE=

# --- REST endpoints (examples; replace with SBI official API paths) ---
SBI_API_BASE_URL=https://api.example.com
SBI_FX_ORDER_PATH=/fx/orders
SBI_FX_POSITIONS_PATH=/fx/positions

# --- WebSocket ---
SBI_WS_URL=wss://stream.example.com
# JSON string to send right after connect. Example:
# {"type":"subscribe","symbol":"USDJPY"}
SBI_WS_SUBSCRIBE_JSON={"type":"subscribe","symbol":"USDJPY"}

# --- Safety / Ops ---
# Maintenance windows in JST, comma-separated HH:MM-HH:MM (local Japan time).
# Default is empty => never blocks.
MAINTENANCE_WINDOWS_JST=

# Logging
LOG_LEVEL=INFO
LOG_PATH=./trader.log
